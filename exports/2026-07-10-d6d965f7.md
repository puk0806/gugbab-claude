# 세션 요약 — 메모리 저장 조건 확인 및 검증

- 세션: `d6d965f7-0d3a-4550-b80b-884592ff796a`
- 기간: 2026-07-10T01:13:06.154Z ~ 2026-07-10T01:49:34.671Z
- 브랜치: `main`
- 도구 사용: Bash 2회 · Read 3회

## 대화

### [1] 요청

메모리 저장 관련해서 Y일떄는 프로젝트 브랜치에 저장,N 일떄는 로컬 PC저장 이거잖아 모든 내용 요약 같은거 해서 강제로 맞나 일단 확인해봐

**응답**

memory 관련 훅과 설정을 직접 확인해서 실제 동작이 어떻게 되어 있는지 보겠습니다.

확인했습니다. 결론부터 말하면 **그 Y/N 모델은 절반만 맞습니다.** "N이면 로컬 PC에 저장"이라는 분기는 존재하지 않아요. 실제 구조는 이렇습니다.

## 실제 동작 (코드로 확인한 내용)

**저장 위치는 애초에 한 곳뿐입니다.**
`~/.claude/projects/.../memory`는 실제 폴더가 아니라 레포의 `memory/` 폴더를 가리키는 **symlink**입니다 (2026-06-11 생성 확인). 즉 memory 파일은 쓰는 순간 무조건 레포 안에 들어가고, "레포 밖 로컬 전용 저장소"라는 경로 자체가 없습니다.

**Y/N 프롬프트의 실제 의미는 "저장 허용 vs 저장 안 함"입니다.**
- **Y (Write/Edit 허용)** → 파일이 레포 `memory/`에 쓰이고, `memory-sync.js` PostToolUse 훅이 즉시 `[memory] sync: <파일명>`으로 **현재 체크아웃된 브랜치**(지금은 main)에 자동 커밋합니다 (`memory-sync.js:37`). push는 하지 않고 로컬 커밋까지만 — push는 사용자가 직접.
- **N (거부)** → Write 자체가 실행되지 않으므로 **아무 데도 저장되지 않습니다.** 로컬에 남는 것도 없습니다.

**보조 장치 2개:**
- `memory-stop-guard.js` (Stop 훅) — 세션 종료 시 미커밋 memory 변경이 남아 있으면 `[memory] auto-sync on stop`으로 커밋. 즉 어떤 경로로든 memory 파일이 변경됐다면 커밋을 피할 수 없습니다.
- `memory-pull.js` (SessionStart 훅) — 세션 시작 시 원격 최신 memory를 pull.

## 정리

| 사용자 이해 | 실제 |
|---|---|
| Y → 프로젝트 브랜치에 저장 | ✅ 맞음 (단, "프로젝트 브랜치"가 아니라 **현재 브랜치**에 로컬 커밋, push는 수동) |
| N → 로컬 PC에 저장 | ❌ 틀림 — N이면 **저장 자체가 안 됨** |

"모든 내용 요약해서 강제 저장" 같은 동작도 없습니다. 훅은 요약을 만들지 않고, 이미 Write/Edit된 memory 파일을 git commit만 합니다. 저장할 내용(개별 fact 파일 + MEMORY.md 인덱스 한 줄)을 만드는 건 훅이 아니라 Claude 본체이고, 그 시점에 permission 프롬프트가 뜨는 겁니다.

### [2] 요청

N 경우 로컬 PC에 강제로 그 전역 클로드 메모리공간 있잖아 거기에 강제로 저장되게 하고 Y일 경우에 현재 브랜치에도 저장하고 N일떄 처럼 전역 클로드 메모리에도 저장해줘 그리고 자동 커밋은 제외해줘 사용해보니 깃 트리가 꼬여서 불편한거 같아

**응답**

구조 변경 작업이라 먼저 현재 훅 구성을 마저 확인하고 계획을 제시하겠습니다.

현재 구조를 전부 파악했습니다. 계획을 제시하기 전에 한 가지 기술적 제약을 먼저 말씀드려야 합니다.

**이렇게 이해했습니다:**
1. 전역 클로드 메모리 공간(`~/.claude/projects/<프로젝트 해시>/memory`)을 symlink가 아닌 **실제 로컬 디렉토리**로 되돌려서, 메모리는 항상 여기에 저장 (= N이어도 로컬에는 남음)
2. 동시에 레포 `memory/`에도 복사(미러링)해서 현재 브랜치에서 커밋 가능하게 (= Y면 브랜치에도 반영)
3. 훅의 **자동 git commit 전부 제거** — 커밋 여부는 사용자가 직접 결정 (깃 트리 꼬임 방지)

**제약 하나:** 권한 프롬프트에서 N(거부)을 누르면 Write 자체가 실행되지 않아 내용이 어디에도 생성되지 않습니다. 훅으로 "거부된 내용을 강제 저장"하는 건 불가능해요. 대신 전역 디렉토리는 권한 프롬프트 없이 쓰이는 공간이므로, **symlink를 끊으면 메모리 저장 자체가 프롬프트 없이 항상 전역에 저장**되고, 레포 반영은 훅이 워킹트리에 복사만 해두고(커밋 안 함) 사용자가 커밋할지(Y) 버릴지(N) 결정하는 구조가 됩니다. 결과적으로 원하시는 동작과 동일합니다.

**작업 목록:**
1. **마이그레이션**: 기존 symlink 제거 → 전역 memory를 실제 디렉토리로 생성 → 현재 레포 `memory/` 파일 전부 복사 (데이터 유실 없음)
2. **memory-pull.js 재작성**: symlink 생성 로직 제거. 세션 시작 시 레포 `memory/`에만 있고 전역에 없는 파일을 전역으로 복사(fresh clone 대비). git fetch/checkout/commit 로직 전부 제거
3. **memory-sync.js 재작성**: 전역 memory 파일 Write/Edit 감지 → 레포 `memory/`로 **파일 복사만** (git add/commit 제거)
4. **memory-stop-guard.js 제거**: 존재 이유가 "종료 전 자동 커밋"이므로 삭제 (테스트 파일 포함), settings.json 훅 등록 해제
5. **문서 동기화**: `.claude/rules/memory-sync.md` 재작성, CLAUDE.md 훅 설명 1줄 수정, `scripts/setup-memory-link.sh` 정리
6. 관련 memory 파일(`project_hook_diet_plan.md` 등에 훅 개수 언급 있으면) 확인·갱신

진행할까요?
